import { FormattingMdDirective } from './formatting-md.directive';

describe('FormattingMdDirective', () => {
  it('should create an instance', () => {
    const directive = new FormattingMdDirective();
    expect(directive).toBeTruthy();
  });
});
