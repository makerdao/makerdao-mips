import { MdObserveVisibilityDirective } from './md-observe-visibility.directive';

describe('MdObserveVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new MdObserveVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
