import { MdTooltipDirective } from './md-tooltip.directive';

describe('MdTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new MdTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
