import { FilterListHostDirective } from './filter-list-host.directive';

describe('FilterListHostDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterListHostDirective();
    expect(directive).toBeTruthy();
  });
});
