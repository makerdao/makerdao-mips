import { TagMipsetPipe } from './tag-mipset.pipe';

describe('TagMipsetPipe', () => {
  it('create an instance', () => {
    const pipe = new TagMipsetPipe();
    expect(pipe).toBeTruthy();
  });
});
